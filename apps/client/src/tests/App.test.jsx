import { render, screen } from '@testing-library/react'; import { describe, expect, test } from 'vitest'; import App from '../App.jsx';
describe('guest assistant UI', () => { test('renders redesigned welcome message, question input, and starter prompt', () => { render(<App />); expect(screen.getByText(/Welcome to Harborlight/i)).toBeInTheDocument(); expect(screen.getByLabelText(/Ask a hotel question/i)).toBeInTheDocument(); expect(screen.getByRole('button', { name: /Do you have rooms available/i })).toBeInTheDocument(); }); });
